[package]
name = "ast"
version = "0.1.0"
edition = "2024"

[features]
default = []
miette = ["dep:miette"]

[dependencies]
bumpalo = { version = "3.20.3", features = ["collections"] }
smol_str = "0.3.6"
miette = { version = "7.6.0", optional = true }
