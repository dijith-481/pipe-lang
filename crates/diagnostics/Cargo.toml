[package]
name = "diagnostics"
version = "0.1.0"
edition = "2024"

[dependencies]
ast = { version = "0.1.0", path = "../ast", features = ["miette"] }
lexer = { version = "0.1.0", path = "../lexer" }
parser = { version = "0.1.0", path = "../parser" }
miette = "7.6.0"
thiserror = "2.0.18"
