[workspace]
members = ["crates/*"]
resolver = "2"

[workspace.lints.rust]
warnings = "deny"

[workspace.lints.clippy]
correctness = "deny"
suspicious = "warn"
style = "warn"
perf = "warn"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
